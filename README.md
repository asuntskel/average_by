# Average-by utility for Crystal Enumerable(T)
Adds some basic enumerable.average_by {|e| e.property }, heavily inspired on the crystal source code
for other similar functions

To use, simply add the `average_by.cr` to your project source files and require it when needed.
